Directory for keep training and simulation datasets.

Dataset should end with a .dat file format.

The datasets can be downloaded via the command:

./scripts/download_data.py

The user will then be prompted to enter a secret key to download
data.
